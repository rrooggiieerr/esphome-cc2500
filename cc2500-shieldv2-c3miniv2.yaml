# Configuration for CC2500 Shield V2.0 on Wemos C3 mini V2.1.0

external_components:
  - source: github://rrooggiieerr/esphome-cc2500@main
    components: [cc2500]

spi:
  id: cc2500_spi
  clk_pin: GPIO1
  mosi_pin: GPIO4
  miso_pin: GPIO0

cc2500:
  spi_id: cc2500_spi
  cs_pin:
    number: GPIO2
    ignore_strapping_warning: True
  # gdo0_pin: GPIO3 # Temperature
  gdo2_pin: GPIO5 # Interrupt
  output_power: 0xFF

# Temperature sensor is currently not supported on ESP32
# sensor:
#   - platform: cc2500
#     temperature:
#       name: CC2500 Temperature
#       update_interval: 5s
#       entity_category: diagnostic
#       disabled_by_default: True
